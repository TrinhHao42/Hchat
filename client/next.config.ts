import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'export',
  // images: {
  //   unoptimized: true, // bỏ qua Image Optimization API
  // },
  reactStrictMode:false
  /* config options here */
};

export default nextConfig;
